{
  "name": "sgraal-web-static",
  "version": "1.0.0",
  "private": true,
  "description": "Sgraal landing page (static). Tailwind build toolchain for the CDN→built-CSS migration (canary: index.html only).",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "0.1.1",
    "@tailwindcss/forms": "0.5.10",
    "autoprefixer": "10.4.20",
    "postcss": "8.4.49",
    "tailwindcss": "3.4.17"
  }
}
